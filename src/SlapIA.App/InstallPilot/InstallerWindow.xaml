<Window x:Class="InstallPilot.InstallerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Installation" Height="450" Width="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource bgBrush}">

    <Window.Resources>
        <ResourceDictionary Source="InstallPilotStyles.xaml"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="110"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource surfaceBrush}">
            <TextBlock x:Name="TxtTitle" Text="Installation — 0 application(s)" VerticalAlignment="Center" Margin="24,0" FontFamily="Segoe UI" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource fgBrush}"/>
        </Border>

        <!-- Divider -->
        <Border Grid.Row="1" Background="{DynamicResource borderBrush}"/>

        <!-- Apps ScrollList -->
        <ScrollViewer Grid.Row="2" Background="{DynamicResource bgBrush}" VerticalScrollBarVisibility="Auto" Margin="0,4">
            <StackPanel x:Name="StackApps" Margin="16,8"/>
        </ScrollViewer>

        <!-- Divider -->
        <Border Grid.Row="3" Background="{DynamicResource borderBrush}"/>

        <!-- Footer -->
        <Border Grid.Row="4" Background="{DynamicResource surface2Brush}">
            <Grid Margin="24,14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Global Progress -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <ProgressBar x:Name="GlobalProgBar" Height="10" Width="340" HorizontalAlignment="Left" Minimum="0" Maximum="1" Value="0"
                                 Foreground="{DynamicResource accentBrush}" Background="{DynamicResource borderBrush}">
                        <ProgressBar.Template>
                            <ControlTemplate TargetType="ProgressBar">
                                <Grid x:Name="TemplateRoot" SnapsToDevicePixels="true">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="5"/>
                                    <Grid x:Name="PART_Track">
                                        <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" CornerRadius="5" HorizontalAlignment="Left"/>
                                    </Grid>
                                </Grid>
                            </ControlTemplate>
                        </ProgressBar.Template>
                    </ProgressBar>
                    <TextBlock x:Name="TxtGlobalStatus" Text="0 / 0" VerticalAlignment="Center" Margin="352,0,0,0" FontFamily="Segoe UI" FontSize="11" Foreground="{DynamicResource fg2Brush}"/>
                </Grid>

                <!-- Buttons Row -->
                <Grid Grid.Row="1">
                    <Button x:Name="BtnSaveScript" Content="Sauvegarder le script (.bat)" Height="34" Width="190" HorizontalAlignment="Left" Click="BtnSaveScript_Click" IsEnabled="False"/>
                    <Button x:Name="BtnAction" Content="Annuler" Height="34" Width="100" HorizontalAlignment="Right" Click="BtnAction_Click"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
