<Window x:Class="InstallPilot.StartupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InstallPilot - Scan" 
        Height="150" Width="380"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False">

    <Window.Resources>
        <ResourceDictionary Source="InstallPilotStyles.xaml"/>
    </Window.Resources>

    <Border Background="{DynamicResource bgBrush}" BorderBrush="{DynamicResource borderBrush}" BorderThickness="1" CornerRadius="12">
        <Border Background="{DynamicResource surfaceBrush}" CornerRadius="12" Margin="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock x:Name="TxtTitle" Grid.Row="0" Text="Analyse des applications"
                           FontFamily="Segoe UI" FontSize="15" FontWeight="Bold"
                           Foreground="{DynamicResource fgBrush}" Margin="0,0,0,8"/>

                <!-- Progress text -->
                <TextBlock x:Name="TxtProgress" Grid.Row="1" Text="Analyse 0/0 : ..."
                           FontFamily="Segoe UI" FontSize="11"
                           Foreground="{DynamicResource fg2Brush}"
                           VerticalAlignment="Center" Margin="0,0,0,8"
                           TextTrimming="CharacterEllipsis"/>

                <!-- Progress Bar -->
                <ProgressBar x:Name="ProgBar" Grid.Row="2" Height="6" Minimum="0" Maximum="1" Value="0"
                             Foreground="{DynamicResource accentBrush}" Background="{DynamicResource borderBrush}">
                    <ProgressBar.Template>
                        <ControlTemplate TargetType="ProgressBar">
                            <Grid x:Name="TemplateRoot" SnapsToDevicePixels="true">
                                <Border Background="{TemplateBinding Background}" CornerRadius="3"/>
                                <Grid x:Name="PART_Track">
                                    <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" CornerRadius="3" HorizontalAlignment="Left"/>
                                </Grid>
                            </Grid>
                        </ControlTemplate>
                    </ProgressBar.Template>
                </ProgressBar>
            </Grid>
        </Border>
    </Border>
</Window>
