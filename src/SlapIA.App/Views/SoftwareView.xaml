<UserControl x:Class="SlapIA.App.Views.SoftwareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:loc="clr-namespace:SlapIA.App.Services"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Width="320" HorizontalAlignment="Left">
                <TextBox Style="{StaticResource SearchBoxStyle}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="{loc:Tr Software_SearchPlaceholder}"
                           IsHitTestVisible="False" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource LabelText}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SearchText}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
            <TextBlock Grid.Column="1" Text="{Binding CountText}" VerticalAlignment="Center" Style="{StaticResource LabelText}"/>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="0">
            <DataGrid ItemsSource="{Binding Applications}"
                      Style="{StaticResource SoftwareGridStyle}"
                      ColumnHeaderStyle="{StaticResource SoftwareGridHeaderStyle}"
                      CellStyle="{StaticResource SoftwareGridCellStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{loc:Tr Software_ColumnName}" Binding="{Binding Name}" Width="2*"/>
                    <DataGridTextColumn Header="{loc:Tr Software_ColumnVersion}" Binding="{Binding Version}" Width="*"/>
                    <DataGridTextColumn Header="{loc:Tr Software_ColumnPublisher}" Binding="{Binding Publisher}" Width="1.5*"/>
                    <DataGridTextColumn Header="{loc:Tr Software_ColumnInstallDate}" Binding="{Binding InstallDate, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
