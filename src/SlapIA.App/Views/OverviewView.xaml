<UserControl x:Class="SlapIA.App.Views.OverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:loc="clr-namespace:SlapIA.App.Services"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600">

    <UserControl.Resources>
        <Style x:Key="InfoCard" TargetType="Border" BasedOn="{StaticResource CardBorder}"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <UniformGrid Columns="3" Rows="2">
                <UniformGrid.Resources>
                    <Thickness x:Key="CellMargin">0,0,16,16</Thickness>
                </UniformGrid.Resources>

                <Border Style="{StaticResource InfoCard}" Margin="0,0,16,16">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{loc:Tr Overview_Computer}" Style="{StaticResource LabelText}"/>
                        <TextBlock Text="{Binding ComputerName}" Style="{StaticResource ValueText}" Margin="0,4,0,2"/>
                        <TextBlock Style="{StaticResource LabelText}">
                            <Run Text="{loc:Tr Overview_User}"/><Run Text="{Binding UserName, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource InfoCard}" Margin="0,0,16,16">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{loc:Tr Overview_OperatingSystem}" Style="{StaticResource LabelText}"/>
                        <TextBlock Text="{Binding OsName}" Style="{StaticResource ValueText}" Margin="0,4,0,2"/>
                        <TextBlock Style="{StaticResource LabelText}">
                            <Run Text="{loc:Tr Overview_Version}"/><Run Text="{Binding OsVersion, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource InfoCard}" Margin="0,0,0,16">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{loc:Tr Overview_Uptime}" Style="{StaticResource LabelText}"/>
                        <TextBlock Text="{Binding Uptime}" Style="{StaticResource ValueText}" Margin="0,4,0,2"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource InfoCard}" Margin="0,0,16,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{loc:Tr Overview_Processor}" Style="{StaticResource LabelText}"/>
                        <TextBlock Text="{Binding ProcessorName}" Style="{StaticResource ValueText}" Margin="0,4,0,2"/>
                        <TextBlock Text="{Binding ProcessorDetails}" Style="{StaticResource LabelText}"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource InfoCard}" Margin="0,0,16,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{loc:Tr Overview_Memory}" Style="{StaticResource LabelText}"/>
                        <TextBlock Text="{Binding MemoryTotal}" Style="{StaticResource ValueText}" Margin="0,4,0,2"/>
                        <TextBlock Text="{Binding MemoryDetails}" Style="{StaticResource LabelText}"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource InfoCard}">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{loc:Tr Overview_GraphicsCard}" Style="{StaticResource LabelText}"/>
                        <TextBlock Text="{Binding GraphicsCardName}" Style="{StaticResource ValueText}" Margin="0,4,0,2"/>
                        <TextBlock Text="{Binding GraphicsCardDetails}" Style="{StaticResource LabelText}"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <UniformGrid Columns="2" Margin="0,16,0,0">
                <Border Style="{StaticResource InfoCard}" Margin="0,0,16,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{loc:Tr Overview_PrimaryDisk}" Style="{StaticResource LabelText}"/>
                        <TextBlock Text="{Binding PrimaryDisk}" Style="{StaticResource ValueText}" Margin="0,4,0,2"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource InfoCard}">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{loc:Tr Overview_Motherboard}" Style="{StaticResource LabelText}"/>
                        <TextBlock Text="{Binding Motherboard}" Style="{StaticResource ValueText}" Margin="0,4,0,2"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
